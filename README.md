# Pathfinders - Электронный журнал клуба "Следопыт"

[![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.1.2-brightgreen.svg)](https://spring.io/projects/spring-boot)
[![Java](https://img.shields.io/badge/Java-17-orange.svg)](https://openjdk.java.net/)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-15-blue.svg)](https://www.postgresql.org/)
[![Docker](https://img.shields.io/badge/Docker-ready-blue.svg)](https://www.docker.com/)

## Описание

Pathfinders - это веб-приложение для управления электронным журналом детского клуба "Следопыт". Система позволяет директору клуба управлять учениками, проводить занятия и выставлять оценки.

## Основные возможности

- 🔐 **Аутентификация и авторизация** с использованием Spring Security
- 👥 **Управление учениками** - добавление, редактирование, удаление
- 🏫 **Управление клубами** - создание и настройка клубов
- 📚 **Проведение занятий** - планирование и учет посещаемости
- ⭐ **Выставление оценок** - оценка успеваемости учеников
- 📊 **Отчеты и аналитика** - просмотр статистики

## Технологический стек

### Backend
- **Java 17**
- **Spring Boot 3.1.2**
  - Spring Data JPA
  - Spring Security
  - Spring Web
  - Spring Validation
  - Spring Actuator
- **Liquibase** - миграции базы данных
- **Lombok** - уменьшение boilerplate кода
- **ModelMapper** - маппинг между DTO и Entity

### Frontend
- **Thymeleaf** - серверный шаблонизатор
- **Bootstrap** - CSS фреймворк

### База данных
- **PostgreSQL 15** - production база данных
- **H2** - база данных для разработки и тестирования

### DevOps
- **Docker & Docker Compose**
- **Maven** - управление зависимостями и сборка

## Архитектура проекта

```
pathfinders/
├── src/
│   ├── main/
│   │   ├── java/ru/otus/hw/
│   │   │   ├── config/          # Конфигурация Spring
│   │   │   ├── controller/      # REST и Web контроллеры
│   │   │   ├── dto/             # Data Transfer Objects
│   │   │   ├── entity/          # JPA Entity классы
│   │   │   ├── exception/       # Кастомные исключения
│   │   │   ├── repository/      # Spring Data JPA репозитории
│   │   │   ├── security/        # Конфигурация безопасности
│   │   │   └── service/         # Бизнес-логика
│   │   └── resources/
│   │       ├── db/changelog/    # Liquibase миграции
│   │       ├── static/          # Статические ресурсы
│   │       ├── templates/       # Thymeleaf шаблоны
│   │       └── application.yml  # Конфигурация приложения
│   └── test/                    # Unit и интеграционные тесты
├── docker-compose-app.yml       # Docker Compose для приложения
├── docker-compose-psgr.yml      # Docker Compose для PostgreSQL
├── Dockerfile                   # Образ приложения
└── pom.xml                      # Maven конфигурация
```

## Быстрый старт

### Предварительные требования

- Java 17 или выше
- Maven 3.6+
- Docker и Docker Compose (опционально)
- PostgreSQL 15+ (если не используете Docker)

### Установка и запуск

#### Вариант 1: Запуск с Docker Compose (рекомендуется)

```bash
# Клонируйте репозиторий
git clone https://github.com/kozionov/Pathfinders.git
cd Pathfinders

# Скопируйте файл с переменными окружения
cp .env.example .env

# Отредактируйте .env файл при необходимости
nano .env

# Запустите PostgreSQL
docker-compose -f docker-compose-psgr.yml up -d

# Соберите и запустите приложение
docker-compose -f docker-compose-app.yml up -d
```

Приложение будет доступно по адресу: http://localhost:8080

#### Вариант 2: Локальный запуск

```bash
# Клонируйте репозиторий
git clone https://github.com/kozionov/Pathfinders.git
cd Pathfinders

# Запустите PostgreSQL (или используйте существующий)
docker-compose -f docker-compose-psgr.yml up -d

# Соберите проект
mvn clean package

# Запустите приложение
java -jar target/pathfinders-0.1.jar

# Или используйте Maven плагин
mvn spring-boot:run
```

#### Вариант 3: Разработка с H2 (in-memory)

```bash
# Запустите с профилем dev
mvn spring-boot:run -Dspring-boot.run.profiles=dev
```

### Учетные данные по умолчанию

**Директор клуба:**
- Логин: `dir`
- Пароль: `dir`

⚠️ **Важно:** Смените пароль по умолчанию в production!

## API документация

### Основные endpoints

| Метод | Endpoint | Описание | Требует auth |
|-------|----------|----------|-------------|
| GET | `/` | Главная страница | Нет |
| GET | `/login` | Страница входа | Нет |
| POST | `/login` | Аутентификация | Нет |
| GET | `/clubs` | Список клубов | Да |
| POST | `/clubs` | Создать клуб | Да |
| GET | `/students` | Список учеников | Да |
| POST | `/students` | Добавить ученика | Да |
| GET | `/lessons` | Список занятий | Да |
| POST | `/lessons` | Провести занятие | Да |
| GET | `/actuator/health` | Статус приложения | Нет |

### Actuator endpoints

- `/actuator/health` - статус приложения
- `/actuator/info` - информация о приложении
- `/actuator/metrics` - метрики приложения

Подробную API документацию можно посмотреть через HAL Explorer: http://localhost:8080/explorer

## Конфигурация

### Переменные окружения

Основные переменные окружения (см. `.env.example`):

```env
# Database
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=pathfinders_db
POSTGRES_USER=pathfinders_user
POSTGRES_PASSWORD=secure_password

# Application
SPRING_PROFILE=prod
SERVER_PORT=8080

# Security
JWT_SECRET=your-secret-key-change-this
JWT_EXPIRATION=86400000
```

### Профили Spring

- `dev` - разработка (H2 database, debug логи)
- `prod` - production (PostgreSQL, оптимизированные настройки)
- `test` - тестирование (in-memory H2, тестовые данные)

## Разработка

### Запуск тестов

```bash
# Все тесты
mvn test

# Интеграционные тесты
mvn verify

# С покрытием кода (JaCoCo)
mvn clean test jacoco:report
```

### Code Style

Проект использует Checkstyle для контроля качества кода:

```bash
mvn checkstyle:check
```

### Создание миграций

Добавьте новый changelog в `src/main/resources/db/changelog/`:

```yaml
databaseChangeLog:
  - changeSet:
      id: <timestamp>-<description>
      author: <your-name>
      changes:
        - createTable:
            tableName: new_table
            columns:
              - column:
                  name: id
                  type: bigint
```

## Deployment

### Production deployment

```bash
# 1. Соберите Docker образ
docker build -t pathfinders:latest .

# 2. Запустите с production конфигурацией
docker-compose -f docker-compose-psgr.yml -f docker-compose-app.yml up -d

# 3. Проверьте логи
docker-compose logs -f pathfinders-app

# 4. Проверьте health
curl http://localhost:8080/actuator/health
```

## Roadmap

### В разработке
- ✅ Базовая функциональность журнала
- ✅ Аутентификация и авторизация
- ✅ Docker поддержка

### Планируется
- [ ] Валидация данных на всех формах
- [ ] Улучшенная обработка ошибок на клиенте
- [ ] Страница логина с восстановлением пароля
- [ ] REST API для мобильных приложений
- [ ] Система уведомлений (email/SMS)
- [ ] Экспорт отчетов в PDF/Excel
- [ ] Множественные роли (учитель, родитель, ученик)
- [ ] Интеграция с календарем
- [ ] Мобильное приложение

## Troubleshooting

### Проблемы с подключением к БД

```bash
# Проверьте статус PostgreSQL
docker-compose -f docker-compose-psgr.yml ps

# Проверьте логи
docker-compose -f docker-compose-psgr.yml logs

# Переподключитесь к БД
docker-compose -f docker-compose-psgr.yml restart
```

### Приложение не запускается

```bash
# Проверьте Java версию
java -version  # Должна быть 17+

# Очистите и пересоберите
mvn clean install

# Проверьте порты
netstat -ano | findstr :8080
```

## Вклад в проект

Мы приветствуем ваш вклад! Пожалуйста, ознакомьтесь с [CONTRIBUTING.md](CONTRIBUTING.md) для получения подробной информации.

### Как внести вклад

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/amazing-feature`)
3. Commit изменений (`git commit -m 'Add some amazing feature'`)
4. Push в branch (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## Лицензия

Этот проект создан в образовательных целях.

## Контакты

- GitHub: [@kozionov](https://github.com/kozionov)
- Проект: [Pathfinders](https://github.com/kozionov/Pathfinders)

## Благодарности

- OTUS за образовательную программу
- Spring Team за отличный фреймворк
- Сообщество разработчиков за поддержку

---

⭐ Если проект оказался полезным, поставьте звезду на GitHub!